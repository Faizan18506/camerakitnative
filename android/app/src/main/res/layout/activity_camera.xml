<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Reverting back to ViewStub as required by Camera Kit Session.attachTo() -->
    <ViewStub
        android:id="@+id/camera_kit_stub"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Flip Camera Button (Top Right) -->
    <ImageButton
        android:id="@+id/camera_flip_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="40dp"
        android:layout_marginEnd="20dp"
        android:background="@android:drawable/screen_background_dark_transparent"
        android:padding="8dp"
        android:src="@android:drawable/ic_menu_rotate"
        android:tint="#FFFFFF"
        android:contentDescription="Flip Camera" />

    <!-- Go Live Button (Below Flip Button) -->
    <Button
        android:id="@+id/btn_go_live"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="100dp"
        android:layout_marginEnd="20dp"
        android:text="GO LIVE"
        android:backgroundTint="#FF0000"
        android:textColor="#FFFFFF" />

    <!-- Custom Carousel: RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/lenses_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="110dp"
        android:layout_gravity="bottom"
        android:layout_marginBottom="25dp"
        android:clipToPadding="false"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:background="@android:color/transparent" />

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:indeterminateTint="#FFFC00"
        android:visibility="gone" />

</FrameLayout>
